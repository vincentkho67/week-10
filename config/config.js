const config = {
    development: {
        username: "root",
        password: "root",
        database: "todos",
        host: "localhost",
        dialect: "mysql"
    }
}

module.exports = config;